# Claude Code 项目规则

本文件是 Claude Code 在本工程中的项目级协作规则。用户的明确指令优先于本文件。
通用 AI 协作规则与 [AGENTS.md](AGENTS.md) 保持一致；两份文件发生调整时必须同步更新，
不得让 Claude Code 与其他 AI 使用不同的架构、安全或迁移标准。

## 作用范围

- 本规则适用于 `media-platform:rebuild` 工程及其子目录。
- 相邻 `../calles` 是迁移来源，不得修改、格式化、删除或重命名，除非用户明确要求。
- 现有未提交变更视为用户工作，禁止擅自回退或覆盖；开始工作前先检查 `git status`。

## 语言与沟通

- 协作沟通、设计文档、迁移记录、ADR、验收报告和面向项目成员的说明默认使用中文。
  用户未明确要求其他语言时，使用中文讨论、提问、汇报和交接。
- 新增面向最终用户的页面文案、接口提示和业务错误消息默认使用中文；不得因翻译改变既有
  错误码、字段或响应语义。
- 代码标识符、Java 包名/类名/方法名、数据库列名、配置键、HTTP Header、事件名、路由键、
  文件名和第三方技术术语保持英文或行业惯例，不强行中文化。

## 面向用户的解释与决策支持

本节适用于讨论、方案、审查、实施进度和交付说明。目标是保留分析深度，同时让用户不必
记住整个工程也能理解判断、比较取舍并决定下一步；不是单纯缩短回答或降低技术准确性。

### 先补齐理解所需的背景

- 默认用户了解业务目标，但没有刚读过相关代码，也未必记得服务、字段、旧方案和历史决策。
  不将 AI 已读取的项目上下文当成双方共有知识；不能用“前面已经说过”替代必要解释。
- 引入与判断有关的模块或机制时，先用一两句话说明“它负责什么、当前怎么运行、与本问题
  有什么关系”，再给出问题和建议。只补当前功能点所需背景，不从头复述整个项目。
- 首次使用影响理解的术语、缩写或项目内部名称时，就地解释其作用；不要只展开英文全称。
  先讲业务行为，再给技术名称。文件路径和源码链接是证据，不是正文解释的替代品。
- 明确区分已核实的现状、尚待验证的推断、目标设计和自己的建议。不把“代码存在”说成
  “已经可用”，不把拟议方案说成已经决定。

### 给出明确建议，而不是只列问题

- 对需要决策的问题，先用简短结论说明推荐方向，再按需要展开相关现状、问题及其实际影响、
  推荐做法与理由、代价或限制、下一步。顺序可自然调整，不要求每次机械输出全部标题。
- 不能停在“存在风险”“需要完善”“建议解耦”等抽象判断。说明什么条件下会出问题、
  用户或业务会看到什么结果、建议具体改变什么；复杂因果关系优先用一个小例子或简短流程解释。
- 有多种可行方向时，明确推荐一个并解释为什么适合当前阶段；只在确有实质取舍时列出
  一两个备选项及适用条件，不把没有判断的选项列表交给用户自行猜测。
- 推荐动作说明涉及哪个服务或文档、先做什么、预期得到什么、如何判断完成。不强迫用户
  从大段分析中自己提炼执行步骤，也不以“都可以”“取决于需求”回避已有证据支持的判断。
- 保持当前授权阶段：讨论只给方向和下一步，不擅自生成完整实施计划；正式方案才展开
  实现、验证和回退；提出下一步建议不等于获得执行授权。

### 在真正影响方向的地方确认

- 讨论或方案结束时，若仍有影响目标、范围、业务体验或风险取舍的决定，先给推荐意见，
  再提出一至三个具体确认问题。说明该答案会改变什么，避免只问“你怎么看”“还有问题吗”。
- 能通过仓库勘察确认的事实先自行查清；纯实现细节不要全部抛给用户。用户需要判断的是
  目标和取舍，而不是替 AI 选类名、查路径或解释已有代码。
- 用户已经确认的方向和已授权的低风险实施不重复索要确认；没有实际决策点时不为凑格式
  强行追问。出现实质偏差或高风险操作时，仍按原有授权边界暂停确认。

### 控制输出层次与密度

- 正文优先让用户看懂并能行动；实现细节、文件证据和验证记录放在解释之后，按需展开。
  简单问题简短回答，复杂问题允许充分解释；不靠堆术语、文件名、缩写或过度压缩制造专业感。
- 进度更新只讲新发现、它的影响和当前动作，不逐项播报工具调用；交付先讲实际变化和使用影响，
  再给验证与未验证项，不把文件数量、行数或命令清单当成主要成果。
- 发送前自检：没读过相关代码的人能否理解“现状是什么、为什么要改、推荐怎么做、接下来
  谁做什么、需要确认什么”？若必须靠猜才能接上因果，应先补说明，而不是让用户翻文件补课。

## 注释与标注

- 新增或实质修改的代码，只要涉及类、接口、枚举、Record、属性、构造方法或普通方法，就必须
  补充简洁、准确的中文注释或 JavaDoc，包括实际参与实现的私有单元。
- 类、接口、枚举和 Record 说明职责、所属边界、主要协作对象和不应承担的工作；类属性说明
  业务含义、生命周期、单位/取值约束、默认值、可空性或敏感性（适用时）；构造方法和方法说明
  用途、参数、返回值、异常、权限、幂等性和副作用（适用时）。
- 方法过程中的关键步骤必须在对应代码块前添加中文步骤注释，说明处理顺序、业务原因、状态
  转换、数据一致性、安全判断、外部调用、重试/降级或失败出口。关键步骤包括校验、权限判断、
  事务边界、持久化、缓存读写、消息发布/消费、远程调用和资源清理。
- 注释优先解释“为什么这样做、约束是什么、失败时会怎样”，而不是逐句翻译显而易见的代码。
  简单 getter/setter 或一眼可懂的局部变量可使用简短标注，但涉及业务含义的属性和方法不能省略。
  若需要大段注释才能理解，应先改善命名、拆分和结构。
- 配置、环境变量、脚本和基础设施编排中的分组、变量关联、默认值原因和迁移过渡策略同样需要
  中文注释。逻辑变化时同步更新受影响的注释，禁止保留过期、作者署名或模板化注释。

## 架构边界

- 跨服务、认证、消息或数据库变更前，先阅读 `docs/architecture.md` 与 `docs/migration-plan.md`。
- 服务只访问自己拥有的表。禁止跨服务 Mapper、跨库 SQL Join、共享领域实体或复制其他服务
  的业务实现。
- `common-core` 只放无业务含义的基础契约；`common-web` 只放 Web 横切能力，不得放共享业务层。
- 客户端只能通过网关访问服务。新增外部接口使用 `/api/<domain>/...`，并同步网关路由与文档。
- 文件业务 API 同样只能经网关；MinIO 对象 PUT/GET 仅可作为经 ADR 与部署确认的短期预签名受控例外，
  不能借此开放服务直连、匿名 bucket 或伪造身份 Header。
- 非即时流程优先使用 RabbitMQ 领域事件；消费者必须幂等，事件必须版本化。

## 功能点协作阶段

默认围绕一个具体功能点推进，例如文件能力、RAG 服务链、认证刷新或审核事件；先讨论清楚，
再按需要深入实现。除非用户明确要求，不预先规划或实现其他领域。

| 用户意图 | 应做的事 | 默认禁止 |
| --- | --- | --- |
| “讨论”“构思”“分析” | 勘察现状，讨论目标、边界、取舍和待确认问题 | 编码、改配置、生成完整实施方案 |
| “构建方案”“出方案” | 形成可执行方案，列出实现、验证和回退 | 修改业务代码、数据库或部署状态 |
| “检查方案”“审方案” | 审查架构、兼容性、数据、安全和测试缺口 | 按方案实施或顺手改代码 |
| “执行”“实现”“do” | 按已通过方案进行最小范围实现和验证 | 静默扩大范围或改变确认方向 |
| “验收”“检查实现” | 独立对照方案检查变更并运行必要验证 | 未经说明直接修复问题 |

标准回合为 `讨论 -> 独立方案 -> 方案审查 -> 实施 -> 独立验收`。用户只授权某一阶段时，只完成
该阶段；只有明确要求完整回合时才连续推进。审查和验收优先交给未参与上一阶段的会话；无法
独立验收时必须说明剩余风险。实施发现边界、数据、契约、安全、依赖或工作量有实质偏差时，
暂停并回到讨论或方案阶段。验收默认只读，问题带证据回流实施，不在验收阶段顺手修复。

## 实施流程

本节只适用于已经进入实施阶段的任务：

1. 确认范围：读取相关代码、配置和文档，确认目标模块、调用方和影响边界。
2. 检查现状：检查工作区变更和相关测试；重叠的用户修改必须保留，无法安全合并时停止说明。
3. 最小实施：只修改完成当前目标所需的文件，不夹带无关重构、依赖升级或全仓格式化。
4. 同步契约：API、JWT、事件、配置、路由、表所有权或服务职责变化时同步更新文档；新增环境
   变量时更新 `.env.example`，但不得写入真实敏感值。
5. 风险验证：至少编译受影响模块；配置变更验证 YAML 和服务启动；接口、认证、数据或消息变更
   补充或执行对应测试/启动探针，并记录命令和结果。
6. 交付说明：说明改动、受影响服务、验证结果、未验证项、偏差和遗留风险。

## 操作授权边界

可自主执行：读取代码和配置，修改当前工程源代码与文档，添加可逆测试，执行构建/测试/静态校验，
启动短时本地探针，补充非敏感本地示例配置。

必须先获得用户明确确认：

- 修改相邻单体 `../calles`，或删除、覆盖、迁移其文件和数据。
- 删除表/字段/数据，执行不可逆批量更新，或修改生产、共享或未知环境的数据库。
- 切换网关流量、启用/停用双写、发布/重放生产消息，或改变对外 API 的兼容性。
- 轮换 JWT 密钥、修改认证授权语义、暴露管理端点，或输出真实凭证和用户数据。
- 引入基础设施、付费服务或大范围依赖/框架替换。

高风险操作在请求确认前，先给出影响范围、步骤、验证和回退方案。

## 契约与兼容性

- 对外 HTTP API、服务间 API、RabbitMQ 事件和 JWT Claims 都是版本化契约；遵循
  `docs/contracts/README.md`，记录所有调用方或消费者。
- 删除/重命名字段、改变字段语义或类型、收紧校验、改变鉴权要求或错误语义，均属破坏性变更。
  必须发布新版本，提供兼容期、下线日期和迁移指引。
- 事件消费者必须容忍未知字段；生产者不得在消费者完成迁移前停止旧事件版本。
- API、事件和 Token 变更测试必须覆盖兼容场景。

## 数据库与跨服务调用

- 表结构变更必须提交可审查、可重复执行的迁移脚本，写明兼容策略、回填方式和回退限制；禁止
  依赖应用启动时自动建表或隐式改生产结构。
- 迁移优先采用“新增 -> 回填 -> 双读/双写 -> 切换 -> 稳定观察 -> 删除旧结构”；删除需确认、
  备份和回退说明。大批量回填须可分批、可重试、可记录进度并校验结果。
- 一个表、Redis Key 或对象存储元数据只能有一个业务所有者，所有权转移要更新架构文档和 ADR。
- 新增同步调用前确认异步事件不能满足需求；同步调用声明超时、失败语义、重试上限和降级方式。
  禁止无界级联调用、远程分布式事务和把非幂等操作直接重试。
- 本地事务不能覆盖远程调用或消息发布；跨服务一致性使用事件、Outbox 或补偿流程。

## 可观测性与数据保护

- 每个入站请求和领域事件必须携带或生成 `traceId`，并在下游调用、日志和事件中传播。
- 日志、异常、测试输出和监控标签不得包含密码、完整 JWT、访问密钥、身份证件、完整邮箱或
  手机号；必要时掩码或记录不可逆摘要。
- 新增接口、消费者或定时任务时定义成功量、失败量或延迟/积压指标，并说明健康检查或告警入口。

## 迁移完成与架构决策

- 服务只有在边界、数据所有权、契约、旧适配范围、回填/双写/切流/回退、认证授权、幂等、限流、
  测试、探针、监控和旧实现删除条件均已验证或记录后，才能标记为已迁移或进入切流。
- 新服务、数据所有权转移、共享模块、基础设施、同步调用、跨服务事件、JWT 策略、双写/切流或
  核心框架变更，必须在 `docs/adr/` 新增 ADR，使用 `0000-template.md` 模板。
- ADR 记录背景、决策、备选方案、影响、风险、验证和回退；已接受决策只能由新 ADR 替代，不能
  静默改写历史结论。

## 安全、工程与构建

- JWT 使用 `Authorization: Bearer <token>`；密钥、密码、Token 和真实用户数据不得写入代码、
  测试、README、`.env.example` 或 Nacos 示例。
- 第一阶段 MySQL 使用服务专属表前缀：`auth_`、`user_`、`content_`、`file_`、`audit_`、`interaction_`、
  `recommend_`。开始迁移前完成接口、表、Redis Key、事件和回退盘点。
- Java 目标版本为 17，沿用现有 Spring Boot、Spring Cloud 和 MyBatis-Plus 版本；保持
  Controller、Application/Service、Repository/Mapper 分层。
- 当前根目录包含 `:`。Linux 下不要直接执行根 reactor 的 `./mvnw clean package`；全量构建使用
  `./build.sh -DskipTests`，单服务构建使用 `./mvnw -f service/<service-name>/pom.xml package -DskipTests`。
- 修改服务配置后验证 YAML 并重新打包；修改认证或网关后启动服务并探测 HTTP 接口。Docker 不可用
  时先执行 `docker compose config -q`，不要把容器启动失败误判为工程配置错误。
